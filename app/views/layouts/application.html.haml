!!!
%html
  %head
    %title BLiNQ Message Board
    = stylesheet_link_tag    'application', media: 'all'
    = javascript_include_tag 'application'
    = csrf_meta_tags
  %body
    -# = render partial: 'layouts/navbar'
    .row
      .col-sm-1
      .col-sm-10
        .flash
          - flash.each do |key, msg|
            = content_tag :div, msg, :id => key
        = yield
